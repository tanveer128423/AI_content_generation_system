Hey I'm going to create a boiler-plate code for a hiring process. I'm planning  
to build a only frontend react application which is an LLM orchestration using langchain.       
layer. This is a two week's proccess the candidate should do things progressively. Now the end goal of the project to have an Content generation engine that takes inputs, create a learning unit and creates MCQ questions for it. This should be done progressively since we are agile. Now I want you to give me a plan.md for the boiler plate code and the progress. I'm visioning a project that takes the following inputs: course level inputs: course.name , course.outcomes, course.hld. Module level inputs: module.name and module.description. Learning Units level inputs: lu.name, lu.description, lu.learner_journey, lu.duration, lu.artifacts[index_id].artifacts_type, lu.artifacts[index_id].link, lu.additional_guidance. Question level inputs: lu[index_id].questions.total_questions, lu[index_id].questions.easy, lu[index_id].questions.medium, lu[index_id].questions.hard. Prompt related input. prompt.lu_prompt prompt.question_prompt All the state should be stored in a single content_timestamp.json file so that system should support save file and load file for saving the changes and loading the changes to new laptop. No need for backend database, all the state are stored in the one single json file(give me the data structured in the plan.md). This system should be a white_box which means all the system_instructions are fetched from the frontend itself with the respective prompt path and the context are passes via handlebars. Refer to this existing codebase (/home/abraham-jeron/Desktop/KalviumLabs/forge/arthur) which I've built for the prompting stratergy. I want some features to be implemented already so that the candidate should not feel that they are starting from scratch. write test cases for the expected outputs. 